#include "sdlstuff.h"
#include "level.h"

void renderLevel(void) {
  // Sint16 vx[8];
  // int pos = 0;
  // for (int i = 0; i < 8; ++i) {
  //   pos += WIDTH / 8;
  //   vx[i] = pos;
  // }
  // Sint16 vy[4] = {50, 100, 300, 155};
  // bezierRGBA(renderer, vx, vy, 4, 2, 255, 255, 255, 255);
}